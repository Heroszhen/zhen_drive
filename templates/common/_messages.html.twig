{% for message in app.flashes('success') %}
    <div class="alert alert-success mb-1">{{ message }}</div>
{% endfor %}

{% for message in app.flashes('danger') %}
    <div class="alert alert-danger mb-1">{{ message }}</div>
{% endfor %}
